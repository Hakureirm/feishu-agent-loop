# ROADMAP：已实现的边界与待做项

本文的待做项均为 **ASPIRATIONAL**，不表示已成为必经门禁。验收按“源码存在 / 配置启用 / 运行采用 / 实际效果”分别记录。

## 已有基础（1.1）

- 标准插件/skill布局、通知分级与动态心跳说明。
- 有界CLI消费、独立退出码检查、stderr保留、连续三次无输出消费错误停止；失败含非空输出或解析错误时立即保留并停止。
- 配置化发送者白名单和群归属；保留当前事件中存在的reply_to/root_id。
- 基于本地outbox的P2P父消息归属，以及已登记自发消息的回流过滤；未认领原文保留为deferred，默认32条上限，有限运行有deferred时不返回0。
- 发送回执结构/身份校验、带初始化header的0600 outbox、并发锁与幂等登记；零字节/缺header不当成新状态，但不认证来源或检测旧合法快照回滚。附离线正反例与CI接线。

这些检查只负责消息传输与候选筛选，不拥有动作审批或任务完整PASS。

## 1. 同线单消费者与运行采用证明

**未实现：** 当前两个相同line的消费者仍可能处理同一事件。进程列表可以诊断，不能证明执行排他；node/native包装层也不能双计成消费者。

- 设计：`session_id + line + lease generation + expiry`，启动注册，在处理/发送边界做fencing；跨机器需要明确的共享协调服务。
- 验收：同线双实例最多一个获执行资格；旧generation在租约过期后仍运行也不能发送；worker退出后能安全恢复。
- 限制：不要用一个本地PID文件冒充跨机器lease，也不替换别人的在途进程。

## 2. 持久inbox、ACK、去重与发送intent

**未实现：** 当前原始捕获仅用于当前回合和失败修复；路由成功输出后可能被宿主丢失，重连间隙能否补齐取决于CLI/事件服务。没有所有入站消息的重复执行防护。

- 设计：事件持久落盘后才ACK；消费/处理/已确认三种状态分开；按message ID与任务generation去重。
- 发送：先登记pending intent，再发送、记录API回执和readback；隔离发送返回与outbox登记之间的user身份回流窗口。
- 验收：在落盘、发送返回、登记、宿主接收、处理完成各点故意中断；重启后不丢任务、不重复副作用；失败原因和原始证据保留。
- 禁止把`--idempotency-key`或append-only文件单独称作exactly-once。

## 3. 上下文与审批收据

**未实现：** 候选路由不解析人类意图，也不认证任意JSON回执的来源。

- 设计：独立审批收据绑定actor、action、target、scope、source、有效期与被回复请求。消息候选、任务接收、发布授权不是同一种PASS。
- source同时支持飞书消息定位和真实终端user turn；peer转述只作线索，不能自授权限。
- 验收：模糊“随意”、错误reply_to、过期确认、伪造本地PASS文件、复制别的任务授权均不允许副作用；明确且范围匹配的真实确认才成立。
- 不把某个固定短语当万能密码；不用修改宿主权限或全局配置绕过人工审批。

## 4. 父消息查询与未知群接入

**部分实现：** outbox能识别本地已知父消息；未知群/未认领P2P只发元数据通知。当前不做自动网络回拉。

- 设计：按当前schema/API用`+messages-mget`查父消息，记录查询身份、完整性和来源，跨会话确认唯一owner；失败仍为unknown。
- 验收：父消息不可见、已删除、请求失败、相同bot名称不同session时均不猜归属；未知群可被观测但不自动授权。
- 旧版本“事件没有root_id”的观察不再外推到所有CLI；以运行版本schema为准。

## 5. 健康、延迟与恢复

**未实现通用健康合同。** 当前脚本只实施已列明的退出码/输入保留规则，不是外部watchdog或长期基础设施。

- 设计：分别记录event ready、最近收信、最近处理、最近发送成功与readback；明确CLI/harness/模型各阶段时间。
- 验收：P2P、@群、非@群逐条真实测试；权限不足或队列延迟不能被“进程在线/累计收到过”掩盖。
- 单次CLI挂死的外部watchdog须识别并只处理自有进程，绝不终止共享总线；实施前先做信号/后代进程负例。
- 资料查询/补偿轮询只有在目标和身份获授权时采用，不默认升级到user读取全部群消息。

## 6. 低噪声汇报和可恢复交接

**已有说明，未自动强制：** Fyi / ShouldSee / MustAck，以及长间隔心跳。

- 设计：工作项绑定task/session、原始证据引用、真实时间、产物指纹和未验证项；区分准备完成、已审核、已发布、已部署。
- 验收：未知主动耗时不填假数；并行agent时间不直接叠加成一个人的wall time；重复记账可检测。
- 停滞检测须区分正常等待、排队、权限阻塞和真正无进展，不因三次正常心跳无改动就自动打扰用户。
- 人读消息给结论和入口；原始证据本地保留，不把敏感日志全文外发。
