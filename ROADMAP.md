# ROADMAP(设计项,未实现)

按价值排。来源:aragorn-connect(通知分级/授权路由)、Aragorn 原则 P2/P6/P11/P13/P15/P31、ADSD。

## 1. 判定权与权限边界(grant_routing)
- 问题:用户在飞书回一句"随意"能不能等于授权部署/删数据?现在没定义。
- 设计:飞书回复只能给**可自主类**授权(装依赖、跑长命令、读分支);**主权类**(部署、动 secret、删仓/分支、花钱)必须回一个固定短语或确认卡片,Agent 把 `{action, risk, reason, msg_id}` 记进事件日志。
- 验收:一条"随意"不能触发主权类动作;一条固定短语能,且日志里能对上 message_id。

## 2. 空转/挂死检测(idle-aware)
- 问题:`/loop` + 心跳只证明"活着",证明不了"在进展"。
- 设计:每轮记一个"产物指纹"(改动文件 hash / run 号 / 指标);连续 N=3 次心跳指纹不变 → 自动发 MustAck 问人,而不是继续烧 token。
- 验收:人为卡住一个任务,3 个心跳内收到 MustAck。

## 3. 会话/话题绑定
- 问题:多任务并行时所有对话混在一个 P2P 会话,人分不清回的是哪个。
- 事实(2026-09-04):`event consume` 吐出的事件顶层字段**没有** `root_id`/`parent_id`,要话题路由得再用 `im +messages-get <message_id>` 查(未验证)。
- 设计:每个任务开一个话题(thread),进度回复带 `root_id`;Monitor 收到事件后查 root_id,按话题路由到对应 `/loop`。

## 4. 输入即数据(安全)
- 问题:bot 进群后,群里任何人的文字都会进 Agent 上下文。
- 已做:SKILL 写死"只把白名单 open_id 当指令"。
- 待做:白名单落配置文件;非白名单消息进上下文时加 `[untrusted]` 前缀;群消息只响应 @bot;对"让 Agent 去做 X"类文本做一次二次确认。

## 5. 与 ADSD 对齐的汇报格式
- 每次唤醒后的动作按 `Intent / Decision / Evidence` 三段回飞书,原始输出贴出来不转述,让飞书聊天记录本身可审计。
- 待做:给 `+messages-send --markdown` 一个模板函数;人读的那份要"说人话"(不用代号/编号)。
